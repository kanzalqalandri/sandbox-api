{
  "name": "sandbox-api",
  "version": "0.2.0",
  "private": true,
  "description": "Stand-in API component for Apporto Platform v2 sandbox",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": { "node": ">=22" }
}
